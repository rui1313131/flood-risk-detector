numpy>=1.24
scipy>=1.10
rasterio>=1.3
# richdem は任意の FillSink バックエンド。
# Windows では C++ ビルドが MSVC で失敗するため自動スキップする
# (sink_detection.py 側で ImportError をハンドリング済み、pure バックエンドが代用)。
richdem>=0.3.4; sys_platform != 'win32'
geopandas>=0.14
shapely>=2.0
osmnx>=1.9
matplotlib>=3.7
contextily>=1.5
pandas>=2.0
fiona>=1.9
pyproj>=3.6
